<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /01-sistema-previsao-tempo/backend/

    # Remover .php da URL
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^(.*)$ index.php [QSA,L]
</IfModule>
